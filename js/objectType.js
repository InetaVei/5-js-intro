const skaiciu = 7;   // number
const teksta = 'labas';      //string
const tiesa = true;         //boolean
const melas = false;        // boolean
const pazymiai = [5, 10];    // object (array)
const zodziai = ['Labas', 'rytas'];   // object (array)

console.log(skaiciu, typeof skaiciu);

console.log(zmogus);
console.log(zmogus['vardas']);



console.log('--------------');

const auto = {
    marke: 'tesla',
    modelis: '3',
    spalva: 'auksine',
    baterija: 100,
    sedyniu: 4
}  
console.log(`mano ${auto.marke} yra ${auto.spalva} ir turi ${auto.sedyniu} sedyniu`);


const katinas = {
    rusis: 'skotu nulepausis',
    spalva: 'juodas',
    svoris: 8,
    charakteris:'geras'
}

console.log(`mano katino rusis ${katinas.rusis}, jo spalva ${katinas.spalva}, jo svoris ${katinas.svoris} kg., jo charakteris ${katinas.charakteris}.`);